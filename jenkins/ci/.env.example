# Copier vers .env sur le serveur CI :
#   cp .env.example .env

JENKINS_HTTP_PORT=8085
JENKINS_AGENT_PORT=50000
SONAR_HTTP_PORT=9000
SONAR_DB_PASSWORD=change-me-sonar-db
SONAR_MEM_LIMIT=3g

# Cache repos optionnel monté dans Jenkins
HOST_REPO_CACHE=/var/lib/sedi-ci/repos
